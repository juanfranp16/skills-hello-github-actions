name: Approved pull request

on:
  pull_request:
    types: closed
jobs:
  tag-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Label pr when approved
        uses: tiagonapoli/label-pr-on-merge@v0.0.2
        with:
          merged-label-name: "mergeau"
          target-branch: "main"
